#include <stdio.h>
#include <math.h>

int function(){
  printf("Sqrt %f\n", sqrt(16));
  printf("Ceil 1.4 is %f\n", ceil(1.4));
  printf("Ceil 1.4 is %f\n", round(1.4));
  printf("Pow 4^3 is %f\n", pow(4, 3));
  return 0;
}
